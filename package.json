{
  "name": "mindblow",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4",
    "uvk": "^1.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "buildDeploy": "GENERATE_SOURCEMAP=false react-scripts build && yarn deploy",
    "deploy": "surge --domain mindblow.surge.sh build/"
  }
}
